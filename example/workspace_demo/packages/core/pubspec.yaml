name: core
description: Core package with build_runner dependency
publish_to: none

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.9
  json_serializable: ^6.8.0
